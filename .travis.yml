language: node_js
node_js:
  - 10.15.0
cache:
  directories:
  - node_modules
  - flow-typed
script:
  # - npm test
  - npm run build
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $github_token
  local_dir: build
  on:
    branch: master